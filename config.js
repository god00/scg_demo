// Application configuration.
'use strict';

var config = module.exports;

config.googleKey = 'your_api_key;
config.googlePlaceEndPoint = 'https://maps.googleapis.com/maps/api/place';
config.textSearchWithquery = '/textsearch/json?query=';

config.line = {
    channelAccessToken: 'your_access_token',
};
